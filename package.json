{
  "name": "auto-post-telegram-api",
  "version": "1.0.0",
  "description": "A Cloudflare Worker API to post to social media channels",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "author": "Sonu Gupta",
  "license": "ISC",
  "dependencies": {
    "openai": "^4.26.0"
  },
  "devDependencies": {
    "wrangler": "^3.22.4"
  }
}
